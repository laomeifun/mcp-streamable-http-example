# Ignore Git directory
    .git
    # Ignore Node.js dependencies (they will be installed inside the container)
    node_modules
    # Ignore compiled output directory (it will be generated inside the container)
    dist
    # Ignore Dockerfile itself and dockerignore
    Dockerfile
    .dockerignore
    # Ignore local environment files
    .env*
    # Ignore OS-specific files
    .DS_Store
    Thumbs.db
    # Ignore log files
    *.log
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*
    # Ignore test files if not needed in production image
    # src/**/*.test.ts
    # src/**/*.spec.ts
